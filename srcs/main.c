#include "cub3d.h"

int	main(int argc, char **argv)
{
	//todo: check if the arguments are correct
	check_args(argc, argv);
	//todo: parse the file to retrieve data, exit if there's an error
	// if (parse_map(argv[1]) == false)
	// 	return (2);
	// sends the data to the raycasting loop
	printf("Bye cub :(\n");
	return (0);
}